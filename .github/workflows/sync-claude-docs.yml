name: Sync Claude Code docs (en)

on:
  workflow_dispatch: {}
  schedule:
    - cron: "0 18 * * *"

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Crawl & save markdown
        run: |
          python crawl_claude_code_docs.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: sync Claude Code docs (en)"
          title: "Sync Claude Code docs (en)"
          body: |
            Claude Code の公式ドキュメントを自動更新しました。
            差分をご確認ください。
          branch: chore/sync-claude-code-docs
          base: main
          add-paths: |
            claude-code-docs/**
          labels: |
            automerge
